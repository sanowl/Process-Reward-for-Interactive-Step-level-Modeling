[project]
name = "prism-agent-rl"
version = "0.1.0"
description = "Process reward modeling prototype for interactive multi-step agents."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
prism = "prism.cli:main"

[tool.pytest.ini_options]
pythonpath = ["."]

